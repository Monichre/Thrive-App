const path = require('path');

module.exports = {
  devtool: 'source-map',
  entry: {
    'test': './test.js'
  },
  output: {
    path: path.resolve(__dirname, 'build'),
    filename: '[name].js'
  },
  module: {
    rules: [{
      test: /\.js$/,
      loader: 'babel-loader',
      query: {
        presets: [
          'es2015',
          'react'
        ]
      }
    }]
  },
  devServer: {
    port: 7000
  }
};